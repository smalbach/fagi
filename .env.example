# Copia este archivo a .env y ajusta. npm run start:dev / migrate / test lo leen solos.
DATABASE_URL=postgres://usuario:clave@localhost:5432/fagi
# Base aparte para los tests del servidor (se BORRA entera en cada test). Sin ella, se saltan.
DATABASE_URL_TEST=postgres://usuario:clave@localhost:5432/fagi_test
# La cuenta que se registre con este email nace aprobada y como admin.
ADMIN_EMAIL=tu@correo.com
PORT=8787
